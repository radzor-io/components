{
  "radzor": "1.0.0",
  "name": "@radzor/smart-contract",
  "version": "0.1.0",
  "description": "Interact with EVM smart contracts via JSON-RPC. Read state, call functions, and send transactions.",
  "languages": ["typescript", "python"],
  "category": "web3",
  "tags": ["smart-contract", "ethereum", "evm", "solidity", "web3", "blockchain"],
  "inputs": [
    {
      "name": "rpcUrl",
      "type": "string",
      "description": "JSON-RPC endpoint (e.g. Infura, Alchemy)",
      "required": true
    },
    {
      "name": "contractAddress",
      "type": "string",
      "description": "Deployed contract address",
      "required": true
    },
    {
      "name": "abi",
      "type": "array",
      "description": "Contract ABI (JSON array)",
      "required": true
    }
  ],
  "outputs": [],
  "events": [
    { "name": "onCallResult", "description": "Fired on successful contract call" },
    { "name": "onError", "description": "Fired on error" }
  ],
  "actions": [
    { "name": "call", "description": "Read-only call to a contract function" },
    { "name": "encodeFunctionData", "description": "Encode function call data for a transaction" },
    { "name": "decodeFunctionResult", "description": "Decode return data from a contract call" }
  ],
  "dependencies": { "packages": {} },
  "composability": {}
}
