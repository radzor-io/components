{
  "radzor": "1.0.0",
  "name": "@radzor/api-key-auth",
  "version": "0.1.0",
  "description": "API key authentication middleware. Generate, validate, hash, and revoke API keys.",
  "languages": ["typescript", "python"],
  "category": "auth",
  "tags": ["api-key", "authentication", "middleware", "security", "auth"],
  "inputs": [
    {
      "name": "headerName",
      "type": "string",
      "description": "HTTP header name for API key (default: 'x-api-key')",
      "required": false
    },
    {
      "name": "prefix",
      "type": "string",
      "description": "API key prefix (default: 'rz_')",
      "required": false
    }
  ],
  "outputs": [],
  "events": [
    { "name": "onValidated", "description": "Fired when an API key is validated" },
    { "name": "onRevoked", "description": "Fired when an API key is revoked" },
    { "name": "onError", "description": "Fired on auth error" }
  ],
  "actions": [
    { "name": "generateKey", "description": "Generate a new API key" },
    { "name": "validateKey", "description": "Validate an API key" },
    { "name": "hashKey", "description": "Hash an API key for storage" },
    { "name": "revokeKey", "description": "Revoke an API key" }
  ],
  "dependencies": { "packages": {} },
  "composability": {}
}
